__pycache__/
*.pyc
*.egg-info/
build/
dist/
.DS_Store

# Real configs hold account ids, profile names and internal hostnames.
# Only config.example.yaml is tracked.
config.yaml
config.*.yaml
!config.example.yaml
*.local.yaml
tunnels.yaml

# Tooling and local notes, not shipped to the repo
.serena/
.claude/
docs/superpowers/

# Runtime state, in case anyone points TUNNELS state at the repo.
state.json
*.log

# scratch file written by scripts/release.py, not committed
.release-notes.md
